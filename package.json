{
  "name": "mizuki-server",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @mizuki/server dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @mizuki/server test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "latest",
    "typescript-eslint": "latest"
  }
}
