/**
 * [阶段 P6] common/decorators/public.decorator
 * [职责] @Public() 公开路由标记
 * [状态] SKELETON — P0a 生成，待 P6 提示词实现
 */

export const Public = (): MethodDecorator & ClassDecorator => {
  return () => undefined;
};
